/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
    images: {
      domains: ['wstatic-prod.pubg.com' , "d3b4yo2b5lbfy.cloudfront.net"],
    },
}

module.exports = nextConfig
